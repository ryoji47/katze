<div class="contents row">
  <% @posts.each do |post| %>
    <div class="content_post" style="background-image: url(<%=  %>);">
      <p><%= post.title %></p>
      <span class="name">
        <%= post.content %>
      </span>
    </div>
  <% end %>
</div>